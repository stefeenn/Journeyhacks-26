**Configure API Key:**
   * Create a new file named `.env` inside the `backend` folder.
   * Add the following line to the file, replacing the text with your actual key:
     ```env
     GEMINI_API_KEY=your_actual_api_key_here
     ```